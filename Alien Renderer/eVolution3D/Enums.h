#pragma once

namespace eVolution3D
{
	enum class Axis
	{
		X, Y, Z
	};

	enum class DrawingMode
	{
		POINT,
		LINE,
		LINE_STRIP,
		LINE_LOOP,
		WIRE_TRIANGLES,
		TRIANGLE_STRIP,
		TRIANGLE_FAN
	};
}
